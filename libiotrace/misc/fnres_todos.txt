



- CMakeLists.txt files (in src + test + test/cunit) --> target_link_options + target_compile_options ??
    -> tests -> remove -DWITH_FILENAME_RESOLUTION (libm won't be required anymore)
    	list(REMOVE_ITEM OPTIONS -DWITH_POSIX_IO)




- `event.c`:
    - FNRES_MAX_FMAP_MAX_FNAMES ??


- TODOs in `fctevent.c`
    - MPI_Wait cleanup filename afterwards (MPI_Request_free cancels or continues request -> delete filename ...)


- Writeout to log not working properly ?


- `logging.h`:
    - NDEBUG-Flag not working (when compiling as Release)
    - `<<libiotrace>>` lib wide logging output format